<div class="container">
  <div class="row">
    <div class="col">
      <h2>ジャンル一覧・追加</h2>

      <div class="feild">
        <%= form_with model: @gener, url: geners_path do |f| %>
          <%= f.label: "ジャンル名", autofocus: true, autocomplete: "ジャンル名" %>
          <%= f.text_field :name %>
          <%= f.submit '新規登録', class: 'btn btn-sm btn-success' %>
        <% end %>
      </div>

      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>ジャンル</th>
            <th colspan="1"></th>
          </tr>
        </thead>

        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
              <td></td>
              <td><%= link_to "編集する", edit_genre_path(genre.id), class: "btn btn-sm btn-success" %></td>
            </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>